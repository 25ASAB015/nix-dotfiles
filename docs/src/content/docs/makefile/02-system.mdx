---
title: Gestión del Sistema
description: Comandos para compilar, aplicar y validar configuraciones de NixOS
---

# Gestión del Sistema

Módulo: `make/system.mk`

Comandos para compilar, probar, aplicar y validar configuraciones de NixOS. Estos son los comandos fundamentales para el mantenimiento del sistema.

## Resumen de Comandos (Prefijo `sys-`)

| Comando | Descripción | Acceso Rápido |
| :--- | :--- | :--- |
| **`make sys-apply`** | Aplicar configuración (Rebuild + Switch) | `make switch` |
| **`make sys-deploy`** | Add + Commit + Push + Apply | `make sync` |
| **`make sys-check`** | Validar sintaxis y flake | `make validate` |
| **`make sys-status`** | Dashboard de salud del sistema | `make health` |

> [!TIP]
> Para ver la lista completa de comandos antiguos y su equivalencia, consulta la [Guía de Aliases](../10-aliases).

---

## Comandos Principales

### `make sys-apply`
**⭐ Comando más usado** - Compila y activa tu nueva configuración.

```bash
make sys-apply
```

**Qué hace:**
1. `make sys-fix-git` - Corrige permisos en el repositorio Git
2. `git add .` - Prepara todos los cambios para commit
3. Compila la configuración del Flake
4. Activa la nueva configuración inmediatamente

---

### `make sys-deploy`
**🚀 Sincronización Total** - Realiza el flujo completo de respaldo y aplicación.

```bash
make sys-deploy
```

**Qué hace:**
1. **`make sys-doctor`** - Verifica y corrige permisos en `~/.config`, `~/.local` y el repositorio Git
2. `make git-add` - Prepara todos los cambios
3. `make git-commit` - Crea respaldo con timestamp (salta si no hay cambios)
4. `make git-push` - Sube cambios al repositorio remoto
5. **`make sys-apply-core`** - Aplica la configuración localmente

**Propósito:** Asegurar que tu configuración esté respaldada en la nube y aplicada localmente en un solo paso.

---

### `make sys-apply-safe`
Valida la configuración y luego aplica los cambios solo si no hay errores.

```bash
make sys-apply-safe
```

**Qué hace:** Ejecuta `make sys-check` y, si es exitoso, procede con `make sys-apply`.

---

### `make sys-apply-fast`
Aplica cambios omitiendo algunas verificaciones internas de NixOS para mayor velocidad.

```bash
make sys-apply-fast
```

---

### `make sys-test`
Prueba tu configuración compilándola y activándola temporalmente. Los cambios se perderán en el próximo reinicio y no se crea una generación de arranque.

```bash
make sys-test
```

---

### `make sys-build`
Compila la configuración sin aplicar cambios reales. Útil para verificar errores antes de un switch.

```bash
make sys-build
```

---

### `make sys-check`
Realiza validaciones profundas (sintaxis, evaluación y linting).

```bash
make sys-check
```

---

### `make sys-status`
**🏥 Dashboard de Salud** - Muestra un resumen del estado del sistema, espacio en disco, servicios fallidos y estado de Git.

```bash
make sys-status
```

---

### `make sys-doctor` (Internal)
Diagnóstico y reparación automática de permisos en directorios de configuración y el repositorio.

**Note:** This command is now automatically executed as part of `make sys-deploy`. You can still call it directly using the alias `make fix-permissions` if you only need to fix permissions without deploying.

```bash
make fix-permissions  # Alias for sys-doctor
```

---

## Comparación de Operaciones

| Comando | Compila | Activa | Persiste | Generación |
|---------|---------|--------|----------|------------|
| `make sys-build` | ✅ | ❌ | N/A | ❌ |
| `make sys-test` | ✅ | ✅ | Solo sesión | ❌ |
| `make sys-apply` | ✅ | ✅ | Permanente | ✅ |
| `make sys-boot` | ✅ | ❌ | Próximo boot | ✅ |

> [!TIP]
> **Workflow recomendado para cambios seguros:**
> `make sys-check` → `make sys-test` → `make sys-apply`
